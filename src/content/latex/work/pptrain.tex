%---
% slug: pptrain
% title: pptrain
% category: product
% eyebrow: TASK-AGNOSTIC LEARNING
% summary: pptrain makes task-agnostic pretraining on synthetic tasks available through one installable workflow. It packages six paper-derived task families, exact reference-parity checks, and downstream transfer evaluation.
% draft: false
% date: 2026-06-18
% rank: 3
%---
\documentclass[11pt]{article}
\usepackage{publication-web}
\begin{document}

\publicationtitle{pptrain, a library for prepretraining}
\publicationauthors{Axym Labs}
\publicationaffiliations{Darmstadt, Germany}

\begin{publicationhead}
\makepublicationtitle
\end{publicationhead}

\publicationfigure[\linewidth]
  {figures/pptrain-nca-rollout.png}{/work/pptrain-nca-rollout.png}
  {A neural cellular automaton evolving over 24 steps.}
  {Four states from a neural cellular automaton rollout generated by pptrain's NCA task family. Each color represents one of ten discrete cell states.}

\publicationabstract{\textbf{Task-agnostic pretraining} trains a model on synthetic or procedural tasks selected for reusable structure rather than for one downstream objective. It seeks patterns that transfer across later tasks, including compositional rules, state transitions, symbolic operations, and compression strategies. Prepretraining is one implementation: the task-agnostic phase runs before training on natural data.}

\publicationactions{%
  \publicationaction{https://github.com/Axym-Labs/pptrain}{View code}
  \publicationaction{https://pypi.org/project/pptrain/}{Open package}%
}

Pre-pretraining is a good idea trapped in an awkward workflow. The basic recipe is to train a language model on synthetic tasks before natural text, using tasks that may teach useful structure. Cellular automata, symbolic transformations, and mathematical primitives all fit this pattern. One hurdle to production use is that most of the work lives in paper-specific code, hidden data generators, and one-off training scripts. A second is verifying that the prepretrainer matches the intended method and improves subsequent training. The package \href{https://github.com/Axym-Labs/pptrain}{\texttt{pptrain}} addresses both:

\section{Ready to use}

Install it via:

\begin{verbatim}
pip install pptrain
\end{verbatim}

and start pre-pretraining with:

\begin{verbatim}
from pptrain import PrePreTrainer, RunConfig, create_task
from pptrain.integrations import HFCausalLMAdapter, HFModelConfig

trainer = PrePreTrainer(
    task=create_task(
        "simpler_tasks",
        {
            "preset": "paper_binary_1m",
            "sequence_count": 256,
            "eval_sequence_count": 64,
            "max_length": 128,
        },
    ),
    model_adapter=HFCausalLMAdapter(
        HFModelConfig(model_name_or_path="sshleifer/tiny-gpt2")
    ),
    run_config=RunConfig(output_dir="runs/initial", max_steps=20),
)

bundle = trainer.fit().load_transfer_bundle()
\end{verbatim}

\texttt{pptrain} packages several task families from the literature as reusable presets:

\begin{itemize}
\tightlist
\item \texttt{nca}: cellular-automata rollouts; tests whether simple dynamical systems can provide useful upstream structure.
\item \texttt{dyck}: balanced-bracket languages; isolates nested and stack-like structure.
\item \texttt{procedural}: abstract programs such as reverse, sort, set, union, and delete; tests algorithmic text transformations.
\item \texttt{simpler\_tasks}: copy, search, set operations, and related symbolic transformations; breaks pretraining into small controlled operations.
\item \texttt{lime}: induction, deduction, and abduction tasks; targets primitives of mathematical reasoning.
\item \texttt{summarization}: synthetic document transformations; separates compression and selection from factual knowledge transfer.
\end{itemize}

The presets are based on suggestions from the original papers and implementation, but can be heavily customized: for example, \texttt{nca} exposes grid size, number of states, patch size, perception and hidden dimensions, rollout stride and initial rollout steps, train/eval rule counts, sequence counts, maximum length, gzip-complexity band, and per-epoch regeneration.

\section{Reference parity}

Production use also requires a correctness check. A changed generator, tokenizer, or serialization no longer tests the mechanism from the paper, so \texttt{pptrain} includes reference-parity utilities that compare local outputs against original-source fixtures for LIME, synthetic summarization, procedural pretraining, and NCA pre-pretraining.

These checks are exact where they should be exact: normalized examples or token sequences must match the reference output.

\section{Empirical testing}

To determine whether a synthetic task transfers in a particular model and training setup, users can run the evaluation and replication utilities through the same interface used for training. The library supports downstream transfer evaluation, compute-matched natural warmups, diagnostic metrics, plots, CSVs, and Markdown reports.

\section{Extensibility}

Users can write their own pre-pretraining logic by implementing \texttt{SymbolicTaskFamily}: define how examples are sampled, executed, serialized, and tokenized. If the task needs a custom dataset or loss path, implement \texttt{Task} directly.

The core abstraction is a task family. It samples an example, executes it if needed, serializes it into tokens, and exposes a tokenizer specification. The remaining path is ordinary model training: build a dataset, train a Hugging Face causal language model, save the checkpoint, and export a transfer bundle.

\end{document}
